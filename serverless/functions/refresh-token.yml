handler: token_service/main/handler.refresh_token
layers:
  - ${ssm:/dataplatform/shared/python-base-layer-arn}
events:
  - http:
      path: refresh
      method: post
      documentation: ${file(serverless/documentation/refresh-token.yml)}
